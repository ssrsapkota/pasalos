# Input Focus

Highlighting the active field.

### Mechanics
- **Border**: Color changes.
- **Ring**: `outline` or `box-shadow` expands (fade in + scale up spread).
- **Label**: Moves from placeholder position to scale-down floating position (Floating Label pattern).
- **Goal**: Clear indication of where typing will occur.
