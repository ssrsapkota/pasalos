# Skeleton Shimmer

The standard placeholder animation.

### Mechanics
- **Visual**: Gray blocks representing text/images.
- **Animation**: A high-light gradient moving across the block (left to right).
- **CSS**: `linear-gradient` background with `background-position` animation.
- **Goal**: Indicate that structure is known, data is loading.
